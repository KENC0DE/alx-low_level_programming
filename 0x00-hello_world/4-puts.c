#include <stdio.h>

/**
 * main - Prints "Programming is like building a multilingual puzzle"
 *
 * Description: The main function is the entry point of the program. It
 * prints the string "Programming is like building a multilingual puzzle"
 * to the console using the printf function. The string is followed by a
 * newline character to ensure that it is printed on a new line. The main
 * function then returns 0 to indicate successful program completion.
 *
 * Return: Always 0 (success)
 */
int main(void)
{
char str = "\"Programming is like building a multilingual puzzle";
puts(str\n);

return (0);
}
