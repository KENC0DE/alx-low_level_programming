#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen - return length of string
 *
 * @s: the sting
 *
 * Return: intiger value
 */
 
int _strlen(char *s)
{
	int ln;

	ln = strlen(s);
	return (ln);
}
